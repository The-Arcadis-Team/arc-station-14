- type: gameMap
  id: zombiesurvival
  mapName: DIN station
  mapPath: /Maps/Survival/sm.yml
  minPlayers: 0
  stations:
    DIN:
      stationProto: StandardStationzombie
      components:
        - type: StationEmergencyShuttle
          emergencyShuttlePath: /Maps/Shuttles/emergency_run.yml
        - type: StationNameSetup
          mapNameTemplate: "DIN station"
        - type: StationJobs
          availableJobs:
            StationEngineer: [ 2, 2 ]
            MedicalDoctor: [ 2, 2 ]
            Passenger: [ -1, -1 ]
